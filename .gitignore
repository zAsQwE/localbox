# Node
node_modules/
npm-debug.log*

# Локальные артефакты (генерируются лаунчером — не публикуются)
config.json
launcher/localbox-settings.json
certs/
*.pem
*.key

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store
Thumbs.db

# Бэкапы патчей игр
*.localbox-backup

# Кэш/временное
cache/
*.part

# Рантайм-данные сервера и модели TTS (качаются/клонируются сами)
storage/
server/tts/*.pt
server/tts/piper/

# Большие веб-клиенты (ассеты Jackbox) — скачиваются отдельно из источников, см. README.
# НЕ коммитим: слишком велики и не наши.
client/
client-ru/
client-fun/
runtime/

# Сборки PyInstaller — НЕ коммитим (быстро устаревают и ломают пути).
# Собирай exe заново (launcher/build-exe.sh) или через GitHub Actions/Releases.
launcher/build/
launcher/dist/
*.spec

# Android-подпроект временно скрыт (в разработке, экспериментальный VPN)
/android/
